{
  "documentationUrl": "https://developer.salesforce.com/docs/atlas.en-us.api_rest.meta/api_rest/",
  "connectionSpecification": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Salesforce Source",
    "type": "object",
    "required": [
      "insight_tenant_id",
      "insight_source_id",
      "salesforce_instance_url",
      "salesforce_client_id",
      "salesforce_client_secret"
    ],
    "additionalProperties": true,
    "properties": {
      "insight_tenant_id": {
        "type": "string",
        "title": "Insight Tenant ID",
        "description": "Tenant isolation identifier for the ingestion layer.",
        "order": 0
      },
      "insight_source_id": {
        "type": "string",
        "title": "Insight Source ID",
        "description": "Instance discriminator (e.g. 'salesforce-acme-prod').",
        "examples": ["salesforce-acme-prod"],
        "order": 1
      },
      "salesforce_instance_url": {
        "type": "string",
        "title": "Instance URL",
        "description": "Salesforce instance URL (e.g. 'https://mycompany.my.salesforce.com'). OAuth token endpoint is derived from this URL.",
        "examples": ["https://mycompany.my.salesforce.com"],
        "order": 2
      },
      "salesforce_client_id": {
        "type": "string",
        "title": "Client ID",
        "description": "OAuth Consumer Key from the Salesforce External Client App (Client Credentials flow).",
        "order": 3
      },
      "salesforce_client_secret": {
        "type": "string",
        "title": "Client Secret",
        "description": "OAuth Consumer Secret from the Salesforce External Client App.",
        "airbyte_secret": true,
        "order": 4
      },
      "salesforce_start_date": {
        "type": "string",
        "title": "Start Date",
        "description": "UTC start for initial incremental sync. Accepts YYYY-MM-DD or YYYY-MM-DDTHH:MM:SSZ. Records older than this are skipped on first run. Omit to fall back to 2 years before now.",
        "format": "date-time",
        "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}(T[0-9]{2}:[0-9]{2}:[0-9]{2}Z)?$",
        "examples": ["2024-01-01", "2024-01-01T00:00:00Z"],
        "order": 5
      },
      "salesforce_stream_slice_step": {
        "type": "string",
        "title": "Stream Slice Step",
        "description": "ISO-8601 duration for concurrent cursor slice width. Smaller = more parallelism, more API calls. Default P30D (30 days).",
        "pattern": "^P(?:\\d+[YMWD])+(?:T(?:\\d+[HMS])+)?$",
        "default": "P30D",
        "order": 6
      },
      "salesforce_lookback_window": {
        "type": "string",
        "title": "Lookback Window",
        "description": "ISO-8601 duration to re-read before the cursor position each sync (accounts for Salesforce SystemModstamp eventual consistency). Default PT10M.",
        "pattern": "^P(?:\\d+[YMWD])*(?:T(?:\\d+[HMS])+)?$",
        "default": "PT10M",
        "order": 7
      },
      "salesforce_num_workers": {
        "type": "string",
        "title": "Concurrent Workers",
        "description": "Max concurrent REST slice fetches, as a numeric string. Clamped to [1, 50] in code. Default 1 (sequential).",
        "default": "1",
        "order": 8
      }
    }
  }
}
