name: github
version: "2026.05.04"
# cdk_image: <not yet published — add full ghcr.io reference when image is built>
type: cdk
schedule: "0 2 * * *"
workflow: sync
dbt_select: "tag:github+"

connection:
  namespace: "bronze_github"
secret:
  required_fields:
    - github_token
    - github_organizations
    - github_start_date
