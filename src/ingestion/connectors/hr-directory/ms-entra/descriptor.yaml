name: ms-entra
version: '1.0'
schedule: 0 5 * * *
dbt_select: tag:ms-entra+
workflow: sync
connection:
  namespace: bronze_ms_entra
secret:
  required_fields:
    - azure_tenant_id
    - azure_client_id
    - azure_client_secret
