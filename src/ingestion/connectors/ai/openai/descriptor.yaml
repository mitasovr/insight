name: openai
version: "2026.05.04"
schedule: 0 2 * * *
dbt_select: tag:openai+
workflow: sync
connection:
  namespace: bronze_openai
secret:
  required_fields:
    - openai_admin_api_key
    - openai_start_date
