name: m365
version: "2026.05.04"
schedule: 0 2 * * *
dbt_select: tag:m365+
workflow: sync
connection:
  namespace: bronze_m365
secret:
  required_fields:
    - azure_tenant_id
    - azure_client_id
    - azure_client_secret
