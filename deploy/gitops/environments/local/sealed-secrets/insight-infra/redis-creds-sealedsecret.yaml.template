---
# Sample SHAPE — see mariadb-creds-sealedsecret.yaml.template in this
# directory for the workflow that generates a real, cluster-specific
# sealed manifest.
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: redis-creds
  namespace: insight-infra
spec:
  encryptedData:
    redis-password: AgB...
  template:
    metadata:
      name: redis-creds
      namespace: insight-infra
    type: Opaque
